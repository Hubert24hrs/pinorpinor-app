<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Permissions are deliberately minimal.

      INTERNET is the only one the app needs at launch. Camera and media access
      are declared but requested contextually by image_picker at the moment the
      member taps "Take a photo" — never on startup. There is no location
      permission: the backend stores a city, and precise coordinates are never
      sent to a client, so the app has nothing to do with GPS.

      POST_NOTIFICATIONS covers the local notifications raised when the app is
      open. It is requested when the member first enables alerts, not at launch.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera is optional: the app is fully usable choosing from the gallery,
         so declaring it required would exclude devices without one. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="Pinorpinor"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Custom scheme: pinorpinor://profile/<username> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pinorpinor" />
            </intent-filter>

            <!--
              Verified App Links. `autoVerify` makes Android check
              https://pinorpinor.com/.well-known/assetlinks.json against this
              app's signing certificate before it will open these links without
              a chooser. Until that file is published with the release
              fingerprint, links open in the browser — see docs/DEPLOYMENT.md.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="pinorpinor.com" />
                <data android:scheme="https" android:host="www.pinorpinor.com" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Needed on Android 11+ for url_launcher to resolve the WhatsApp
             handoff and to open the legal pages in a browser. Without these,
             canLaunchUrl returns false even when the app is installed. -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
